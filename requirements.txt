# Local development dependencies (run generator, Spark job, dbt, dashboard, tests).
# Airflow runs in its own container (see docker/Dockerfile.airflow); it is NOT
# installed here to keep local installs light.

# core data
pandas==2.2.2
numpy==1.26.4
pyarrow==16.1.0

# transformation / warehouse
pyspark==3.5.1
dbt-core==1.8.7
dbt-postgres==1.8.2
psycopg2-binary==2.9.9
SQLAlchemy==1.4.52

# data lake (MinIO / S3)
boto3==1.34.145

# dashboard
streamlit==1.37.1

# config + testing
python-dotenv==1.0.1
PyYAML==6.0.1
pytest==8.2.2
